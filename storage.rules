rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all files (for images, etc.)
    match /{allPaths=**} {
      allow read: if true;
      // Only allow write for authenticated users (you can restrict this further if needed)
      allow write: if request.auth != null;
    }
  }
}
